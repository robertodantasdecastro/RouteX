# Prompt Example: ADR Preparation

Use this prompt when a RouteX change affects multiple surfaces:

```text
Draft an ADR for the proposed RouteX change.
Include context, decision, consequences, and concrete repository impact.
Call out which configs, docs, tests, and runtime surfaces must be updated together.
Do not implement code changes until the ADR scope is clear.
```
